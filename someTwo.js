    
    function checkUsersValid(goodUsers) {
      return function(submittedUsers) {
    	().map( function(x) { return x} )
      };
    }
    
    module.exports = checkUsersValid

    testAllValid = ([
      { id: 2 },
      { id: 1 }
    ])

we want to iterate through the test to see if all of them are in group